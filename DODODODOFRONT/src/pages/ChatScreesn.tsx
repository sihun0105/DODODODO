import {View, Text} from 'react-native';
import React, {useEffect, useRef, useState} from 'react';

interface Message {
  user: string;
  message: string;
}

const ChatScreesn = () => {
  useEffect(() => {}, []);

  return (
    <View>
      <Text>ChatScreesn</Text>
    </View>
  );
};

export default ChatScreesn;
